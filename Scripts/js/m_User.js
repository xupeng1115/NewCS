var app = new Vue({
    el: "#app",
    data: {
        lognFocus:false,
    },
    methods: {
        
    }
})

$(function () {

    // $("body").on("touchstart", ".flo-container", function (event) {

    // });

});


